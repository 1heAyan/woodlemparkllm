'use client';

// Cleaned up: Celebration toasts and confetti animations have been removed.
export function triggerConfetti(_originX = 0.5, _originY = 0.4) {
  // No-op: Confetti animations disabled
}

export function showCelebrationToast(_title: string, _subtitle?: string, _xp = 50) {
  // No-op: Celebration toasts disabled
}

